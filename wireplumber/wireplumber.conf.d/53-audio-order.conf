# Default-sink order hook: headphones > BT speakers > TV > dock screens > computer
wireplumber.components = [
  {
    name = audio-order.lua, type = script/lua
    provides = custom.audio-order
  }
]

wireplumber.profiles = {
  main = {
    custom.audio-order = required
  }
}
