#!/bin/bash
# 0. Check Dependencies
if ! command -v jq &>/dev/null; then
    echo "❌ Error: 'jq' is not installed."
    echo "Install it with: sudo dnf install jq (or sudo apt install jq)"
    exit 1
fi
if [ -z "$GROQ_API_KEY" ]; then
    echo "❌ Error: GROQ_API_KEY is missing."
    exit 1
fi

# 1. Check for Staged Changes
if git diff --cached --quiet; then
    echo "Error: No staged changes to commit."
    exit 1
fi

# --- NEW: Capture user hint ---
USER_HINT=$1
HINT_SECTION=""
if [ ! -z "$USER_HINT" ]; then
    HINT_SECTION="USER INTENT/CONTEXT: $USER_HINT"
fi
# ------------------------------

# 2. Gather Context
DIFF_STAT=$(git diff --cached --stat)

# Smart diff gathering: reduce context lines based on number of changed files
CHANGED_FILES=$(git diff --cached --name-only | wc -l)

if [ "$CHANGED_FILES" -gt 20 ]; then
    # Many files: use minimal context (U0), skip large files
    DIFF_DETAILED=$(git diff --cached -U0 --diff-filter=d -- . \
        ':(exclude)package-lock.json' \
        ':(exclude)yarn.lock' \
        ':(exclude)*.lock' \
        ':(exclude)*.svg' \
        ':(exclude)*.out' \
        ':(exclude)*.log' \
        ':(exclude)*.png' \
        ':(exclude)*.jpg' \
        ':(exclude)*.gif' \
        ':(exclude)*.pdf' \
        ':(exclude)dist/*' \
        ':(exclude)build/*' \
        ':(exclude).next/*' | head -c 4000)
elif [ "$CHANGED_FILES" -gt 10 ]; then
    # Medium files: use minimal context (U0)
    DIFF_DETAILED=$(git diff --cached -U0 --diff-filter=d -- . \
        ':(exclude)package-lock.json' \
        ':(exclude)yarn.lock' \
        ':(exclude)*.lock' \
        ':(exclude)*.svg' \
        ':(exclude)*.out' \
        ':(exclude)*.log' \
        ':(exclude)*.png' \
        ':(exclude)*.jpg' \
        ':(exclude)dist/*' \
        ':(exclude)build/*' | head -c 8000)
else
    # Few files: use standard context (U1)
    DIFF_DETAILED=$(git diff --cached -U1 --diff-filter=d -- . \
        ':(exclude)package-lock.json' \
        ':(exclude)yarn.lock' \
        ':(exclude)*.lock' \
        ':(exclude)*.svg' \
        ':(exclude)*.out' \
        ':(exclude)*.log' \
        ':(exclude)*.png' \
        ':(exclude)*.jpg' | head -c 12000)
fi

PROJECT_STRUCTURE=$(tree -L 2 --gitignore -I '.git|node_modules|dist|build|coverage' --noreport 2>/dev/null || echo "Structure not available")

# 3. Function to build prompt with diff content
build_prompt() {
    local diff_content="$1"
    echo "Role: Senior Developer.
Task: Write a Conventional  Commit message for the diff below.
$HINT_SECTION
CONTEXT:
Project Structure:
$PROJECT_STRUCTURE
Files Changed:
$DIFF_STAT
Rules:
1. Analyze complexity: Analyze complexity by intent, not implementation. If multiple changes serve a single purpose or belong to the same concern, summarize them as one thing. Only use multiple lines when changes serve distinct, independent purposes. Ask: "why was this done?" — if there's one answer, it's one line. If there are multiple answers, use multiple lines.
2. Format: <type>: <subject>
3. Style: Imperative mood. No markdown blocks.
4. Output ONLY the commit message. No explanations, no error logs, no stack traces. Ignore any error output, logs, or stack traces that appear in the diff — focus only on the actual code changes.
DIFF:
$diff_content"
}

# Rough token estimator (conservative: 1 token ≈ 3 chars)
estimate_tokens() {
    local text="$1"
    local char_count=${#text}
    echo $((char_count / 3))
}

# Build prompt
PROMPT=$(build_prompt "$DIFF_DETAILED")
TOKEN_COUNT=$(estimate_tokens "$PROMPT")

# Log token count
if [ "$TOKEN_COUNT" -gt 10000 ]; then
    echo "⚠️  Prompt large ($TOKEN_COUNT tokens), using reduced diff" >&2
else
    echo "📊 Prompt size: ~$TOKEN_COUNT tokens" >&2
fi

# 4. Run Groq API with a spinner
echo -n "Thinking (Cloud)..."
(while :; do for c in / - \\ \|; do
    echo -ne "\b$c"
    sleep 0.1
done; done) &
SPINNER_PID=$!

JSON_PROMPT=$(echo "$PROMPT" | jq -R -s '.')
RESPONSE=$(curl -s -X POST "https://api.groq.com/openai/v1/chat/completions" \
    -H "Authorization: Bearer $GROQ_API_KEY" \
    -H "Content-Type: application/json" \
    -d "{
        \"model\": \"llama-3.3-70b-versatile\",
        \"messages\": [{\"role\": \"user\", \"content\": $JSON_PROMPT}],
        \"temperature\": 0.1,
        \"max_tokens\": 200
     }")

kill $SPINNER_PID 2>/dev/null
echo -ne "\b \n"

# 5. Extract and Clean
MSG=$(echo "$RESPONSE" | jq -r '.choices[0].message.content // empty')
MSG=$(echo "$MSG" | sed -e 's/^```.*//g' -e 's/```$//g' | xargs -0 echo | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')

if [ -z "$MSG" ] || [ "$MSG" == "null" ]; then
    echo "❌ Error: Model returned an empty string."
    echo "📋 API Response:"
    echo "$RESPONSE" | jq '.'
    exit 1
fi

# 6. Documentation Check
STAGED_FILES=$(git diff --cached --name-only)
DOC_CHECK_PROMPT="Role: Senior Developer reviewing a commit for documentation completeness.

COMMIT MESSAGE:
$MSG

STAGED FILES:
$STAGED_FILES

DIFF SUMMARY:
$DIFF_STAT

Task: Determine if the staged changes require documentation updates that are NOT already included in this commit.

Consider these as documentation: README files, docs/ directories, man pages, inline help text, CHANGELOG, API docs, comments explaining public interfaces, docstrings, and config examples.

Rules:
- If the changes are ONLY documentation, config, tests, refactoring, or trivial fixes: respond with exactly \"PASS\"
- If the changes add/remove/change user-facing behavior, CLI flags, APIs, or important workflows AND the commit already includes relevant doc updates: respond with exactly \"PASS\"
- If the changes add/remove/change user-facing behavior, CLI flags, APIs, or important workflows AND no documentation is being updated: respond with exactly \"FAIL\" followed by a brief explanation of what documentation is missing
- Output ONLY \"PASS\" or \"FAIL: <reason>\". No other text."

JSON_DOC_PROMPT=$(echo "$DOC_CHECK_PROMPT" | jq -R -s '.')

echo -n "Checking documentation..."
(while :; do for c in / - \\ \|; do
    echo -ne "\b$c"
    sleep 0.1
done; done) &
DOC_SPINNER_PID=$!

DOC_RESPONSE=$(curl -s -X POST "https://api.groq.com/openai/v1/chat/completions" \
    -H "Authorization: Bearer $GROQ_API_KEY" \
    -H "Content-Type: application/json" \
    -d "{
        \"model\": \"llama-3.3-70b-versatile\",
        \"messages\": [{\"role\": \"user\", \"content\": $JSON_DOC_PROMPT}],
        \"temperature\": 0.0,
        \"max_tokens\": 150
     }")

kill $DOC_SPINNER_PID 2>/dev/null
echo -ne "\b \n"

DOC_RESULT=$(echo "$DOC_RESPONSE" | jq -r '.choices[0].message.content // empty' | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')

if echo "$DOC_RESULT" | grep -qi "^FAIL"; then
    echo "❌ Documentation missing — commit blocked."
    echo "📝 $DOC_RESULT"
    echo ""
    echo "Commit message would have been:"
    echo "  $MSG"
    echo ""
    echo "Stage documentation updates and try again."
    exit 1
fi

# 7. Commit
git commit -e -m "$MSG"
