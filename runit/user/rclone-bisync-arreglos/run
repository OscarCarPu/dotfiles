#!/bin/sh
exec 2>&1
# Initial 60s lets the session settle before the first sync.
sleep 60
while :; do
    rclone bisync \
        "$HOME/docs/relamidos/arreglos" \
        "relamidos-drive:Tía MiLa/arreglos" \
        --verbose
    sleep 300
done
