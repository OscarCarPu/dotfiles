# Seafile CLI credentials — sourced by the `seafile-setup` script.
# Copy to ~/.config/seafile-cli/credentials and chmod 600. Never commit the
# real file (it is git-ignored).
SEAFILE_SERVER="https://cloud.lab-ocp.com"
SEAFILE_USER="you@example.com"
SEAFILE_PASS="your-password-here"
